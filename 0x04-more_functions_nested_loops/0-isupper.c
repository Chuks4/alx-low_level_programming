#include "main.h"
#include <stdio.h>

/**
 * _isupper - a function that checks for uppercase character
 * @c: input
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
char c;
c = 'C';
if (isupper(c))
	putchar("return uppercase\n")
else
putchar("not uppercase")
return(0)
}
